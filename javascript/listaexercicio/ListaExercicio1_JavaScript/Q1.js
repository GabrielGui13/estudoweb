function op(x, y) {
    const soma = x + y
    const sub = x - y
    const mult = x * y
    const div = x / y

return `Soma: ${soma}
Subtracao: ${sub}
Multiplicacao: ${mult}
Divisao: ${div}`
}

console.log(op(5, 5))